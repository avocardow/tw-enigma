# Task ID: 13
# Title: Implement end-to-end CLI integration
# Status: pending
# Dependencies: 12
# Priority: medium
# Description: Create the main CLI integration function that orchestrates the entire scramble integration process.
# Details:
Implement the main integration function in the CLI:

```typescript
// In the CLI codebase
import { copyFileSync, mkdirSync } from 'fs';
import { dirname, join } from 'path';
import { execSync } from 'child_process';

async function integrateScrambleEffect(
  scramblePackagePath: string,
  options: any
): Promise<void> {
  try {
    const buildDir = options.outDir || 'dist';
    const assetsDir = join(buildDir, 'assets');
    const configuredScriptPath = join(assetsDir, 'enigma-scramble.js');
    
    // Ensure assets directory exists
    mkdirSync(assetsDir, { recursive: true });
    
    // Build scramble package if needed
    if (options.buildScramble) {
      console.log('Building scramble package...');
      execSync('pnpm --filter @tw-enigma/scramble build');
    }
    
    // Inject configuration and copy to build output
    await injectScrambleConfiguration(
      scramblePackagePath,
      configuredScriptPath,
      {
        scrambleSpeed: options.scrambleSpeed,
        scrambleDebug: options.scrambleDebug,
        scrambleMode: options.scrambleMode,
        scrambleCharset: options.scrambleCharset
      }
    );
    
    // Inject script into HTML files
    await injectScrambleIntoHtmlFiles(buildDir, configuredScriptPath);
    
    console.log('✅ Scramble effect integration complete');
  } catch (e) {
    console.error('❌ Error integrating scramble effect:', e);
  }
}

// Add CLI command options
function addScrambleOptions(program: any) {
  return program
    .option('--scramble', 'Enable scramble effect')
    .option('--scramble-speed <ms>', 'Scramble speed in milliseconds (50-1000)', parseInt)
    .option('--scramble-debug', 'Enable debug logging for scramble effect')
    .option('--scramble-mode <mode>', 'Scramble mode (currently only "all" supported)')
    .option('--scramble-charset <charset>', 'Character set for scramble effect');
}
```

Update the CLI command handlers to use these functions:

```typescript
// Example command handler
async function handleCssConfigCommand(options: any) {
  // Process scramble flags if present
  if (options.scramble) {
    const scramblePackagePath = await detectScramblePackage();
    if (scramblePackagePath) {
      await integrateScrambleEffect(scramblePackagePath, {
        ...options,
        buildScramble: true
      });
    } else {
      console.warn('⚠️ Scramble effect requires @tw-enigma/scramble package, which was not found. Skipping scramble integration.');
    }
  }
  
  // Continue with normal CSS processing
  // ...
}
```

# Test Strategy:
Test the end-to-end integration with a sample project. Verify that the scramble package is built, configuration is injected, and the script is injected into HTML files. Test with various CLI options to ensure they're correctly passed to the scramble script.

# Subtasks:
## 1. Package Build Orchestration [pending]
### Dependencies: None
### Description: Design and implement the orchestration logic to build the software package, ensuring all dependencies are resolved and build steps are executed in the correct sequence.
### Details:
Define the build pipeline, invoke build tools, and verify that the output artifacts are generated as expected. Success criteria: Build completes without errors, and all expected artifacts are present.

## 2. Configuration Injection Mechanism [pending]
### Dependencies: 13.1
### Description: Develop a robust mechanism to inject configuration values into the package during the build process, supporting both static and dynamic configuration sources.
### Details:
Implement configuration file parsing, environment variable support, and validation logic. Success criteria: All required configuration values are correctly injected and validated in the final build output.

## 3. Script Copying Logic [pending]
### Dependencies: 13.1
### Description: Implement logic to copy necessary scripts (e.g., shell, batch, or Node scripts) into the build output, preserving file permissions and directory structure.
### Details:
Identify required scripts, handle file system operations, and verify integrity post-copy. Success criteria: All scripts are present in the output with correct permissions and paths.

## 4. HTML Injection Process [pending]
### Dependencies: 13.2, 13.3
### Description: Create a process to inject dynamic or static content into HTML files as part of the build, supporting template variables and content blocks.
### Details:
Parse HTML templates, replace placeholders with actual values, and validate resulting HTML for correctness. Success criteria: All HTML files contain the correct injected content and pass validation checks.

## 5. CLI Flag Parsing and Handling [pending]
### Dependencies: 13.1
### Description: Implement comprehensive parsing and handling of CLI flags, ensuring all supported options are validated, defaults are applied, and errors are reported for invalid input.
### Details:
Define supported flags, implement parsing logic, and integrate with build/configuration steps. Success criteria: CLI flags are correctly parsed, and their effects are reflected in the build process.

## 6. Centralized Error Reporting and Handling [pending]
### Dependencies: 13.2, 13.3, 13.4, 13.5
### Description: Develop a centralized error reporting and handling system that captures, logs, and reports errors from all subsystems, providing actionable feedback and halting the process on critical failures.
### Details:
Implement error capture hooks, structured logging, and user-friendly error messages. Success criteria: All errors are consistently reported, logged, and surfaced to the user with clear remediation steps.

