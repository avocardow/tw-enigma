# Task ID: 10
# Title: Implement CLI integration for package discovery
# Status: pending
# Dependencies: 9
# Priority: medium
# Description: Create the CLI integration logic to detect the scramble package and handle its absence gracefully.
# Details:
Implement the package discovery logic in the CLI:

```typescript
// In the CLI codebase
import { existsSync } from 'fs';
import { resolve } from 'path';

async function detectScramblePackage(): Promise<string | null> {
  try {
    // Try to resolve the package path
    const packagePath = resolve(process.cwd(), 'packages-private/scramble/dist/scramble.min.js');
    
    // Check if the file exists
    if (existsSync(packagePath)) {
      return packagePath;
    }
    
    // Alternative resolution methods
    try {
      // Try using require.resolve (works in Node.js environments)
      return require.resolve('@tw-enigma/scramble/dist/scramble.min.js');
    } catch (e) {
      // Package not found via require.resolve
    }
    
    return null;
  } catch (e) {
    console.warn('⚠️ Error detecting scramble package:', e);
    return null;
  }
}

// Usage in CLI command handler
async function handleScrambleFlag(options: any): Promise<void> {
  if (options.scramble) {
    const scramblePackagePath = await detectScramblePackage();
    
    if (scramblePackagePath) {
      console.log('✅ Scramble package found at:', scramblePackagePath);
      await integrateScrambleEffect(scramblePackagePath, options);
    } else {
      console.warn('⚠️ Scramble effect requires @tw-enigma/scramble package, which was not found. Skipping scramble integration.');
      // Continue normal CLI operation without scramble
    }
  }
}
```

# Test Strategy:
Test the package discovery logic with and without the scramble package installed. Verify that it correctly finds the package when available and gracefully handles its absence. Test with different working directories to ensure path resolution works correctly.

# Subtasks:
## 1. Implement Package Path Resolution Logic [pending]
### Dependencies: None
### Description: Develop a function that resolves a package path given a module name and a base directory, following Node.js resolution semantics. The function should handle package.json lookups, consult relevant fields (e.g., exports, main, module), and return the absolute path or null if not found. Ensure memoization for performance and consistency with Node.js require behavior.
### Details:
Technical requirements: Use file system APIs to traverse directories and locate package.json files. Parse package.json and check fields in the correct order (exports, browser, module, main). Memoize results for repeated lookups. Expected outcome: Given a valid module name and base directory, the function returns the resolved absolute path or null if the package cannot be found.

## 2. Handle Missing Package Scenarios and Error Reporting [pending]
### Dependencies: 10.1
### Description: Extend the resolution logic to robustly handle cases where the package cannot be found or is missing required fields. Implement clear error messages or return values for missing packages, invalid package.json, or unresolved entry points. Ensure that error handling does not cause unhandled exceptions and provides actionable feedback.
### Details:
Technical requirements: Detect missing package.json, missing entry fields, or invalid paths. Return null or throw a controlled error with a descriptive message. Log or propagate errors in a way that can be consumed by CLI handlers. Expected outcome: All missing or invalid package scenarios are handled gracefully, with clear and actionable error reporting.

## 3. Integrate Package Resolution with CLI Command Handlers [pending]
### Dependencies: 10.2
### Description: Connect the package path resolution and error handling logic to CLI command handlers. Ensure that CLI commands can invoke the resolution logic, receive results or errors, and display appropriate output to the user. Provide CLI options for specifying base directories or module names, and ensure exit codes and messages reflect resolution outcomes.
### Details:
Technical requirements: Expose resolution logic via CLI commands. Parse CLI arguments for module name and base directory. Display resolved paths or error messages to the user. Set appropriate exit codes for success or failure. Expected outcome: CLI commands correctly resolve package paths, handle errors, and provide clear user feedback.

