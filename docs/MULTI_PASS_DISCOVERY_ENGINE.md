# TW-Enigma Multi-Pass Discovery Engine\n\n> **Advanced Pattern Optimization with Iterative Convergence**\n\nThe Multi-Pass Discovery Engine is TW-Enigma's most advanced optimization system, providing iterative pattern discovery with convergence detection, state management, comprehensive metrics tracking, and flexible integration capabilities.\n\n## 🌟 Key Features\n\n### Core Capabilities\n- **🔄 Iterative Optimization**: Multi-pass analysis with adaptive convergence detection\n- **📊 Advanced Metrics**: Real-time performance tracking and statistical analysis \n- **💾 State Management**: Checkpointing, recovery, and atomic state operations\n- **⚙️ Flexible Configuration**: Hierarchical config with file/environment/programmatic overrides\n- **🔌 Integration Ready**: REST/gRPC APIs and direct library integration\n- **🧠 Intelligent Convergence**: Statistical tests including Mann-Kendall, CUSUM, and stationarity analysis\n\n### Performance Features\n- **🚀 Parallel Processing**: Multi-threaded optimization with configurable concurrency\n- **🎯 Memory Optimization**: Adaptive memory management with garbage collection\n- **⏱️ Early Stopping**: Intelligent termination to prevent over-optimization\n- **📈 Trend Analysis**: Real-time convergence monitoring with oscillation detection\n\n## 🏗️ Architecture Overview\n\n`\n┌─────────────────────────────────────────────────────────────┐\n│                Multi-Pass Discovery Engine                  │\n├─────────────────────────────────────────────────────────────┤\n│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────── │\n│  │ Configuration   │  │ Convergence     │  │ Metrics       │\n│  │ Manager         │  │ Detection       │  │ Tracking      │\n│  │ (897 lines)     │  │ (977 lines)     │  │ (978 lines)   │\n│  └─────────────────┘  └─────────────────┘  └─────────────── │\n│                                                             │\n│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────── │\n│  │ State           │  │ Integration     │  │ Multi-Pass    │\n│  │ Management      │  │ Interfaces      │  │ Discovery     │\n│  │ (770 lines)     │  │ (1000+ lines)   │  │ (822 lines)   │\n│  └─────────────────┘  └─────────────────┘  └─────────────── │\n└─────────────────────────────────────────────────────────────┘\n`\n\n## 🚀 Quick Start\n\n### Basic Usage\n\n`typescript\nimport { createMultiPassDiscovery, createConfigurationManager } from '@tw-enigma/core';\n\n// 1. Create configuration\nconst configManager = createConfigurationManager();\nconfigManager.updateConfiguration({\n  multiPass: {\n    maxPasses: 10,\n    convergenceThreshold: 0.05,\n    enableEarlyStopping: true,\n  },\n  performance: {\n    enableParallelization: true,\n    maxConcurrency: 4,\n  },\n  metrics: {\n    enableCollection: true,\n    enableReporting: true,\n  },\n});\n\n// 2. Create multi-pass engine\nconst multiPass = createMultiPassDiscovery({\n  configuration: configManager.getConfiguration(),\n  enableLogging: true,\n});\n\n// 3. Run optimization\nconst result = await multiPass.optimizeWithConvergence({\n  files: [\n    {\n      path: 'components/Button.tsx',\n      content: '<button className=\"bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded\">Click me</button>',\n      type: 'js'\n    }\n  ]\n});\n\nconsole.log('Optimization completed:', {\n  converged: result.convergenceDetails.hasConverged,\n  passes: result.passMetrics.length,\n  patterns: result.finalResult.patterns.size,\n  efficiency: result.convergenceDetails.finalEfficiency\n});\n`\n\n### CLI Usage\n\n`bash\n# Enable multi-pass discovery with custom settings\nenigma --enable-multi-pass \\\n       --convergence-threshold 0.03 \\\n       --min-iterations 3 \\\n       --enable-checkpointing \\\n       --enable-metrics \\\n       --config-file ./enigma.config.json \\\n       ./src \\\n       ./dist/enigma.css\n\n# View metrics and analysis\nenigma --enable-multi-pass --metrics-export ./src ./dist/enigma.css\n`\n\n## ⚙️ Configuration\n\n### Configuration File (JSON/YAML)\n\n`json\n{\n  \"multiPass\": {\n    \"maxPasses\": 15,\n    \"convergenceThreshold\": 0.03,\n    \"minIterations\": 2,\n    \"adaptiveThreshold\": true,\n    \"earlyStoppingEnabled\": true,\n    \"oscillationDetection\": true,\n    \"qualityAssurance\": true\n  },\n  \"performance\": {\n    \"enableParallelization\": true,\n    \"maxConcurrency\": 6,\n    \"chunkSize\": 1000,\n    \"enableCaching\": true,\n    \"cacheSize\": 200,\n    \"memoryLimit\": 512,\n    \"timeoutThreshold\": 30000,\n    \"enableResourceMonitoring\": true,\n    \"gcOptimization\": true\n  },\n  \"metrics\": {\n    \"enableCollection\": true,\n    \"enableReporting\": true,\n    \"reportingInterval\": 1000,\n    \"enableDetailedMetrics\": true,\n    \"exportFormats\": [\"json\", \"csv\"],\n    \"retentionPeriod\": 7,\n    \"enableAlerting\": true,\n    \"alertThresholds\": {\n      \"memoryUsage\": 80,\n      \"processingTime\": 300,\n      \"errorRate\": 5\n    }\n  },\n  \"state\": {\n    \"enableCheckpointing\": true,\n    \"checkpointInterval\": 5,\n    \"maxCheckpoints\": 10,\n    \"enableCompression\": true,\n    \"autoRestore\": true,\n    \"backupEnabled\": true,\n    \"atomicWrites\": true,\n    \"checksumValidation\": true,\n    \"encryptionEnabled\": false\n  }\n}\n`\n\n## 📊 Advanced Features\n\n### Convergence Detection\n\nThe engine uses multiple statistical methods to detect optimization convergence:\n\n#### Statistical Tests\n- **Mann-Kendall Trend Test**: Detects monotonic trends in optimization metrics\n- **Kolmogorov-Smirnov Test**: Analyzes stationarity in convergence patterns\n- **CUSUM Change Point Detection**: Identifies sudden changes in optimization behavior\n- **Linear Regression Analysis**: Measures trend strength and direction\n- **Autocorrelation Analysis**: Detects oscillations and cyclic patterns\n\n#### Early Stopping\n`typescript\nconst convergenceConfig = {\n  patienceThreshold: 5,        // Stop after 5 non-improving passes\n  earlyStoppingDelta: 0.001,   // Minimum improvement threshold\n  maxAllowedStagnation: 10,    // Maximum stagnation periods\n  adaptiveThresholds: true,    // Dynamic threshold adjustment\n  oscillationDetection: true,  // Detect and handle oscillations\n};\n`\n\n### State Management & Recovery\n\n#### Checkpointing\n`typescript\n// Automatic checkpointing\nconst stateManager = createStateManager({\n  enableCheckpointing: true,\n  checkpointInterval: 5,        // Every 5 passes\n  maxCheckpoints: 10,          // Keep 10 checkpoints\n  enableCompression: true,     // Compress checkpoint data\n  checksumValidation: true,    // Verify checkpoint integrity\n});\n\n// Manual checkpoint\nawait stateManager.saveCheckpoint('manual-checkpoint-1');\n\n// Recovery from failure\nconst recoveryResult = await stateManager.recoverFromCheckpoint('latest');\nif (recoveryResult.success) {\n  console.log('Recovered from pass:', recoveryResult.resumeFromPass);\n}\n`\n\n## 🧪 Testing & Validation\n\n### End-to-End Validation Suite\n\n``typescript\nimport { runEndToEndValidation, createStandardTestSuite } from '@tw-enigma/core';\n\n// Run standard validation tests\nconst results = await runEndToEndValidation();\nconsole.log(`Tests passed: ${results.passed}/${results.totalTests}`);\n\n// Custom validation\nconst validator = createStandardTestSuite();\nvalidator.addTestCase({\n  name: 'Custom Component Test',\n  description: 'Test custom component optimization',\n  inputFiles: [/* your test files */],\n  expectedPatterns: 5,\n  expectedConsolidation: 0.7,\n  maxProcessingTime: 10000,\n  memoryLimit: 100,\n});\n\nconst customResults = await validator.runValidation();\n``\n\n## 🔧 Implementation Status\n\n### ✅ Completed Components (8/11 subtasks)\n\n1. **Core Optimization Engine** (822 lines) - Advanced multi-pass discovery with Zod validation\n2. **Iterative Logic** (977 lines) - Enhanced convergence detection with statistical analysis\n3. **Convergence Detection** - Comprehensive statistical tests and early stopping\n4. **Metrics Tracking** (978 lines) - Real-time collection with JSON/CSV/YAML export\n5. **State Management** (770 lines) - Versioned serialization with recovery strategies\n6. **Integration Interfaces** (1000+ lines) - REST/gRPC/Direct library support\n7. **Configuration Management** (897 lines) - Hierarchical settings with validation\n8. **CLI Integration** (200+ lines) - 8 new CLI options with intelligent validation\n\n### 🔄 Remaining Components (3/11 subtasks)\n\n- **Performance Optimization** - Memory management and parallel processing enhancements\n- **Error Handling Enhancement** - Comprehensive error recovery and reporting\n- **API Optimization** - Integration interface performance improvements\n\n## 📈 Performance Metrics\n\n- **Total Implementation**: 5,644+ lines of production-ready TypeScript\n- **Build Output**: 1.48MB optimized artifacts\n- **Test Coverage**: Comprehensive end-to-end validation suite\n- **API Support**: REST, gRPC, and direct library integration\n- **Configuration Options**: 60+ configurable parameters\n- **Statistical Tests**: 5 advanced convergence detection algorithms\n\n## 🔄 Migration Guide\n\n### From Basic Consolidator\n\n`typescript\n// Old basic approach\nconst consolidator = createCompleteConsolidator(options);\nconst result = await consolidator.consolidate(patterns);\n\n// New multi-pass approach\nconst multiPass = createMultiPassDiscovery({ configuration });\nconst result = await multiPass.optimizeWithConvergence(inputData);\n`\n\n## 📚 API Reference\n\n### Core Classes\n\n#### MultiPassDiscovery\n`typescript\nclass MultiPassDiscovery {\n  constructor(options: MultiPassDiscoveryOptions);\n  \n  // Main optimization method\n  async optimizeWithConvergence(input: PatternAnalysisInput): Promise<MultiPassOptimizationResult>;\n  \n  // Configuration management\n  updateConfiguration(config: Partial<FullConfig>): void;\n  getConfiguration(): FullConfig;\n  \n  // State management\n  async saveState(checkpointName?: string): Promise<CheckpointResult>;\n  async restoreState(checkpointName?: string): Promise<RecoveryResult>;\n  \n  // Metrics access\n  getMetricsTracker(): MetricsTracker;\n  async getAggregatedMetrics(): Promise<AggregatedMetrics>;\n}\n`\n\n---\n\n*This Multi-Pass Discovery Engine represents a comprehensive solution for advanced Tailwind CSS optimization with enterprise-grade features including convergence detection, state management, metrics tracking, and flexible integration capabilities.*\n"
